// 🧩 Destructuring
// Pulls values out of objects/arrays into variables.

const props = { name: "Alice", age: 24 };
const { name, age } = props;

console.log(name); // Alice
console.log(age);  // 24
